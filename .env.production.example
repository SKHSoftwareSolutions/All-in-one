# Not needed if the backend is deployed as a Vercel Python function in this
# same project (api/index.py) -- API calls stay same-origin at /api/...
# Only set this if the backend is deployed to a separate host (Render, Koyeb, etc).
# VITE_API_BASE_URL=https://your-backend-host.example.com

VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
